name: quick_blue_simplify_example
description: Demonstrates how to use the quick_blue_simplify plugin.
publish_to: 'none'
version: 0.0.1+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  quick_blue_simplify:
    path: ../

  cupertino_icons: ^1.0.2
  permission_handler: ^10.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
